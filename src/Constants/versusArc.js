import { CLASS_PREFIX } from './offcharts';

export const MAIN_NAME = `${CLASS_PREFIX}versusarc`;
export const LIQUID_PREFIX = `${MAIN_NAME}-`;
export const RIGHT_ARC = `${MAIN_NAME}-right-arc`;
export const LEFT_ARC = `${MAIN_NAME}-left-arc`;
export const CENTER_LINE = `${MAIN_NAME}-center-line`;
export const TEXT = `${MAIN_NAME}text`;
export const TEXT_VALUE = `${TEXT}-value`;
export const TEXT_POSTFIX = `${TEXT}-postfix`;
export const TEXT_DECIMAL = `${TEXT}-decimal`;
export const TEXT_LEGEND = `${TEXT}-legend`;
export const START_ANGLE = -Math.PI / 2;
export const END_ANGLE = Math.PI / 2;
